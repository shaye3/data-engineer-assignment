apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: input-pvc
  namespace: sensor-pipeline
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
